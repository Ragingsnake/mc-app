#Temp file only for local testing

apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
type: Opaque
stringData:
  password: "supersecurepassword"
  uri: "postgresql://postgres:supersecurepassword@postgres:5432/stories"